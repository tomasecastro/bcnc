# Pruebas de API con Behave

Este proyecto contiene pruebas de API utilizando el framework de BDD `behave`.

## Instrucciones para ejecutar las pruebas

1. Instalar los requisitos:
   ```bash
   pip install behave requests

